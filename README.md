# Abyssal Orbit — 메인트 카드 메이커 v12

## v12 변경사항
- `✦GENRE` 태그 카테고리 정상 추가
  - 편집 드롭다운에 GENRE 표시
  - 미리보기에 `✦GENRE` 별도 줄 표시
  - GENRE 태그 추가/삭제 가능
- v10에서 저장한 JSON 작업 파일과 호환
  - 기존 파일에 GENRE 데이터가 없어도 정상적으로 열림
  - 불러온 뒤 GENRE만 빈 상태로 시작
- v10의 기존 디자인과 기능은 그대로 유지

## 업데이트
압축 해제 후 `index.html`, `styles.css`, `app.js`, `README.md`를
기존 저장소에 업로드하고 `Commit changes`를 누르세요.
